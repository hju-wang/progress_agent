# Day 3 RAG 检索工具 · 验收卷 v0.1

- 前置条件：先完成 [Day 3 代码任务](../../training/day3-rag-tool/README.md)（TODO-3：实现 `score_chunk` 打分），运行通过。
- 考试方式：闭卷。允许看自己的代码、允许运行命令；**不许问 AI 要答案**。
- 提交方式：把你的选项、运行输出、代码/设计答案直接贴回来。

---

**Q1（单选）** RAG（检索增强生成）的正确流程是？

A. 先对文档分块并建立索引 → 根据用户问题检索相关资料块 → 把资料放入上下文让模型基于资料作答
B. 让模型先直接回答，再用检索结果改写自己的答案
C. 不分块，把全部文档原文一次性塞进上下文

对应能力项：A5

答：A

---

**Q2（单选）** `search_knowledge` 的检索结果回填给模型时，正确做法是？

A. 和其他工具一样：`role="tool"`，带上对应的 `tool_call_id`
B. 检索结果比较特殊，`role` 应该用 `"retrieval"`
C. 检索结果不需要回填，模型自己会“记得”问过什么

对应能力项：A4、A5

答：A

---

**Q3（单选）** 知识库里没有与问题匹配的资料（0 命中）时，Agent 应该？

A. 如实告诉用户知识库没有覆盖这个问题，承认不知道
B. 根据已有资料推测一个“最合理”的答案给用户
C. 换一个相关话题回答

对应能力项：A5、E6

答：A

---

**Q4（实操）** 贴出你完成 TODO-3 后的**真实运行输出**（三段都要）：

```bash
python3 training/day3-rag-tool/agent.py "RAG 的流程是什么？"
python3 training/day3-rag-tool/agent.py "工具调用时有哪些消息角色？"
python3 training/day3-rag-tool/agent.py "今天是几号？"
```

验收标准：前两段能看到 `[工具] search_knowledge(...)` 与 `[命中] doc-2-1` /
`doc-1-1`，不再报 `TODO-3`，最终回答基于检索资料并带来源；第三段正常回答日期。

对应能力项：A5、B1、E6

答：

```shell
❯ python3 training/day3-rag-tool/agent.py "RAG 的流程是什么"

--- 第 1 轮 ---
[工具] search_knowledge({'question': 'RAG 的流程是什么'})
[结果] [命中] doc-2-1（score=1）《RAG 检索增强的流程》
RAG（检索增强生成）分三步：先对文档分块并建立检索索引；再根据用户问题检索最相关的资料块；最后把检索到的资料放入上下文，让模型基于资料作答并标注来源，从而减少幻觉。

--- 第 2 轮 ---
[最终回答] 根据知识库资料，**RAG（检索增强生成）** 的流程分为以下三步：

---

### 1️⃣ 文档分块与建立索引
先对知识文档进行**分块**处理，并为每个块建立**检索索引**，以便后续可以快速查找。

### 2️⃣ 信息检索
根据用户的**问题**，从已建立的索引中**检索**出最相关的资料块。

### 3️⃣ 增强生成
把检索到的资料放入模型的**上下文**中，让模型基于这些资料进行作答，并**标注来源**。

---

✅ **核心价值**：通过"先检索、再生成"的方式，让模型基于真实资料作答，从而有效**减少幻觉**（编造内容）的发生。

---

*（来源：doc-2-1《RAG 检索增强的流程》）*

如需更深入理解某个环节，比如分块策略或检索算法的细节，欢迎继续提问！
❯ python3 training/day3-rag-tool/agent.py "工具调用时有哪些消息角色"

--- 第 1 轮 ---
[工具] search_knowledge({'question': '工具调用时有哪些消息角色'})
[结果] [命中] doc-1-1（score=6）《工具调用与消息角色》
工具调用（function calling）过程中有四种消息角色：system 描述系统设定，user 是用户提问，assistant 携带模型回复或工具调用请求，tool 是工具执行结果。
[命中] doc-1-2（score=3）《工具调用与消息角色》
assistant 请求调用工具后，应用必须把每条结果以 role=tool、并带对应 tool_call_id 的消息回填给模型，模型才能基于结果继续回答。

--- 第 2 轮 ---
[最终回答] 根据本地知识库中的资料（doc-1-1、doc-1-2），以下是您的答案：

## 工具调用时的四种消息角色

在**工具调用（function calling）**过程中，共有四种消息角色：

| 角色 | 作用 |
|------|------|
| **system** | 描述系统设定 |
| **user** | 用户提问 |
| **assistant** | 携带模型回复或**工具调用请求** |
| **tool** | 工具执行结果 |

**补充说明**（来源：doc-1-2）：
- 当 assistant 请求调用工具后，应用必须将每条执行结果以 **`role=tool`**、并携带对应的 **`tool_call_id`** 的消息回填给模型，模型才能基于结果继续回答。

来源：doc-1-1、doc-1-2
❯ python3 training/day3-rag-tool/agent.py "今天是几号"

--- 第 1 轮 ---
[工具] get_current_date({})
[结果] 2026-09-09

--- 第 2 轮 ---
[最终回答] 今天是 **2026年9月9日**（星期三）。有什么我可以帮你的吗？
```

---

**Q5（设计与代码）** 回答两个小问：

1. 用一两句讲清你实现的 `score_chunk` 是怎么给资料块打分的？为什么“完全无关的
   问题”得分会接近 0？
   
2. 这套打分只统计共同 token，非常玩具。如果要处理真实的知识库（上万文档），至少
   要替换/补充一个环节。请选一个方向（向量检索、BM25、rerank、更合理的中文分词、
   分块策略、索引更新……）并说清它能解决什么问题。

对应能力项：A5、B2、D5

答：
1：先将我的问题进行 分词 处理，分成几个 token ,然后再检查分词的 token 是否在chunks 中，如果在chunks 中就记一分
2:向量检索：通过将问题和 chunck 转换成高纬度向量，然后通过匹配高纬度向量的距离，来实现语意思匹配,其他的几个我不了解，但是我也很想知道他们分别是什么概念，又是什么原理
---

> 批注区（教练填写）

**教练批注（2026-09-09）**

- Q1–Q3：全对（A / A / A）。Q2 题干乱码已修复。
- Q4：通过。离线 mock 复跑确认：RAG 流程命中 doc-2-1；消息角色命中 doc-1-1（score=6）；日期功能正常。试卷所贴为真实 API 运行输出，命中与来源标注符合验收。
- Q5.1：通过——先把问题分词，再统计问题 token 中有多少个也出现在资料块里，无共同 token 得 0 分。
- Q5.2：通过（方向正确）——向量检索把问题与资料块转成高维向量、按相似度做语义匹配；已补充 BM25 / rerank / 中文分词 / 分块策略 / 索引更新的概念讲解。
- 结论：**Day 3 验收通过**。诚实记录：README 要求的三道口头复述未在对话中作答（用户选择直接收尾）；相关概念已由 Q2/Q3/Q5 覆盖，Day 6 独立检验仍会抽查。
